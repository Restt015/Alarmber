# ==================================
# SERVER CONFIGURATION
# ==================================
PORT=5000
WS_PORT=5001
NODE_ENV=development

# ==================================
# DATABASE - MongoDB Atlas
# ==================================
# REQUIRED: MongoDB connection string
# 
# Local Development (MongoDB local):
# MONGODB_URI=mongodb://localhost:27017/alarmber
#
# Production (MongoDB Atlas - RECOMMENDED):
# Get this from: MongoDB Atlas → Clusters → Connect → Connect your application
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
#
# Example:
# MONGODB_URI=mongodb+srv://alarmber-admin:YOUR_PASSWORD@alarmber.abc123.mongodb.net/alarmber?retryWrites=true&w=majority
#
# IMPORTANT: If password contains special characters (#, @, %, etc.), URL-encode them:
# # = %23, @ = %40, % = %25, etc.
#
MONGODB_URI=mongodb://localhost:27017/alarmber

# ==================================
# SECURITY - JWT Configuration
# ==================================
# REQUIRED: JWT secret for token signing (minimum 32 characters)
# Generate a secure secret with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
JWT_SECRET=REPLACE_THIS_WITH_A_SECURE_RANDOM_STRING_AT_LEAST_32_CHARACTERS_LONG
JWT_EXPIRE=7d

# ==================================
# FILE UPLOAD CONFIGURATION
# ==================================
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# ==================================
# CORS CONFIGURATION
# ==================================
# Comma-separated list of allowed origins
# Development (Expo):
ALLOWED_ORIGINS=http://localhost:19000,http://localhost:19002

# Production - Add your deployed URLs:
# ALLOWED_ORIGINS=https://your-app.com,exp://u.expo.dev/your-project-id
